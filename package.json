{
  "name": "agentshield-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node --test packages/sdk-ts/dist/**/*.test.js packages/integrations/langchain/dist/**/*.test.js packages/policy-packs/dist/**/*.test.js"
  }
}
